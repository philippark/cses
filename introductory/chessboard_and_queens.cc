#include <bits/stdc++.h>
using namespace std;

/*
just explore all possibilities.

go through, if can place, place. explore. unplace. 

when place a queen. mark that row and col as used.
rows, cols vectors of 8. 

if can place:
not *
and rows[r] and cols[c] is not 1.

can just go through each row

explore()
*/

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);


}